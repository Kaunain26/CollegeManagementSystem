package `in`.kit.college_management_system.studentSection

import androidx.appcompat.app.AppCompatActivity

class StudentHomePage : AppCompatActivity() {

    /* private lateinit var binding: ActivityHomeBinding

     override fun onCreate(savedInstanceState: Bundle?) {
         super.onCreate(savedInstanceState)
         binding = ActivityHomeBinding.inflate(layoutInflater)
         setContentView(binding.root)

         MakeStatusBarTransparent().transparent(this)

         binding.bottomNavigation.add(MeowBottomNavigation.Model(1, R.drawable.ic_home))
         binding.bottomNavigation.add(MeowBottomNavigation.Model(2, R.drawable.ic_check_circle))
         binding.bottomNavigation.add(MeowBottomNavigation.Model(3, R.drawable.ic_school))

     }*/
}