<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.kit.college_management_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollegeManagementApp">
        <activity
            android:name=".facultySection.activity.StudentInCLassActivity"
            android:exported="true" />
        <activity
            android:name=".facultySection.activity.FacultyHomePage"
            android:exported="true" />
        <activity
            android:name=".hodSection.HODHomePage"
            android:exported="true" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".singin_signup.SignInSignUpHostActivity"
            android:exported="true" />
        <activity
            android:name=".studentSection.StudentHomePage"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>